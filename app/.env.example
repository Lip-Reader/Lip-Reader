# Clerk publishable key (pk_...). Leave empty for a guest-only build.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
# Optional backend overrides. Dev falls back to localhost:8000 / :8001,
# production to same-origin / the Modal VSR URL.
# EXPO_PUBLIC_API_BASE=
# EXPO_PUBLIC_VSR_API_BASE=
